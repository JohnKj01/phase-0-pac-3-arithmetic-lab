a=int
b=int
var b=any
function add() {
    parseInt(a, b), 'adds two numbers and returns the result'
    return a+b;
  }
  function subtract() {
    subtract(a, b), 'subtracts b from a and returns the result'
    return a-b;
  }
  function multiply() {
    multiply(a, b), 'multiplies two numbers and returns the result'
    return a*b
  }
  function divide() {
    divide(a, b), 'divides a by b and returns the result'
    return 12/3;
  }
  function increment(n) {
    return (n+1)
  }
  function decrement(n) {
    return (n-1)
  }